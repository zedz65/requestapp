from application import app
# object called app in our init in folder called application

if__name__== '__main__main':
# if this is being run and not imported then
    app.run(port=5001, host='0.0.0.0')
# here we specified a port. host is open to all
